```
=================================================================
              运维退化趋势分析 — 需求分解
=================================================================

一、执行器跟踪误差退化（中期信号，传动退化）

  数据源: actuator_tracking.speed_error_max
  聚合:   按 robot_id + 天，取 MAX
  方法:   7日滑动平均 → 线性回归
  判定:   斜率 > 0 且 p < 0.05 → 退化
  输出:   退化速率(m/s/天)、预计超阈值天数
  含义:   电机磨损 → 指令跟随响应变差

二、到点定位误差退化（晚期信号，综合结果）

  数据源: arrival_record.loc_error_norm
  聚合:   按 robot_id + 天，取 AVG
  方法:   7日滑动平均 → 线性回归
  判定:   斜率 > 0 且 p < 0.05 → 退化
  输出:   退化速率(m/天)、预计超阈值天数
  含义:   不管什么原因，最终都体现为到点精度持续下降

三、扭动概率退化（早期信号，轮系磨损）

  数据源: twisting_info.has_twisting
  聚合:   按 robot_id + 天，有扭动任务数/总任务数
  方法:   7日滑动平均 → 线性回归
  判定:   斜率 > 0 且 p < 0.10 → 退化（放宽抓早期信号）
  输出:   扭动概率上升速率(%/天)
  含义:   驱动轮磨损 → 摩擦下降 → 打滑增多


退化链条:  扭动↑(2-4周预警) → 执行器误差↑(1-2周预警) → 到点误差↑(3-5天预警)

=================================================================
```
